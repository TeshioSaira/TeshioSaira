name: Test GitHub Actions

on:
  schedule:
    - cron: "*/10 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Hello from GitHub Actions!"